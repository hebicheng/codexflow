<UserControl
    x:Class="CodexFlowWindows.Controls.SessionCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CodexFlowWindows.Controls">
    <Border CornerRadius="8" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
        <Grid RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="3">
                    <TextBlock x:Name="NameText" Text="" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                    <TextBlock x:Name="CwdText" Text="" FontFamily="Consolas" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <controls:StatusBadge x:Name="Badge" Grid.Column="1" />
            </Grid>

            <TextBlock x:Name="PreviewText" Grid.Row="1" Text="" TextWrapping="Wrap" MaxLines="2" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
                <TextBlock x:Name="BranchText" Text="" FontSize="12" />
                <TextBlock x:Name="UpdatedText" Text="" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock x:Name="ApprovalText" Text="" FontSize="12" Foreground="{ThemeResource SystemFillColorCautionBrush}" />
            </StackPanel>

            <TextBlock x:Name="SourceText" Grid.Row="3" Text="" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </Grid>
    </Border>
</UserControl>
